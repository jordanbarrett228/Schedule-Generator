{
  "name": "schedule-generator",
  "version": "1.0.0",
  "description": "Standalone Schedule Generator Desktop Application",
  "main": "electron/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "cd frontend && npm run build -- --watch",
    "dev:electron": "wait-on frontend/dist/index.html && nodemon --watch frontend/dist --watch electron --exec \"electron . --dev\"",
    "dev:watch": "cd frontend && npm run build -- --watch",
    "build:frontend": "cd frontend && npm run build",
    "build:python": "cd backend && pyinstaller --clean run_app.spec",
    "build": "npm run build:frontend && electron-builder",
    "build:portable": "npm run build:frontend && electron-builder --dir",
    "postinstall": "electron-builder install-app-deps"
  },
  "build": {
    "appId": "com.schedulegenerator.app",
    "productName": "Schedule Generator",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "electron/**/*",
      "frontend/dist/**/*",
      "backend/**/*",
      "!backend/build/**/*",
      "!backend/dist/**/*",
      "!backend/__pycache__/**/*",
      "!backend/**/__pycache__/**/*"
    ],
    "extraResources": [
      {
        "from": "resources/schedule-backend",
        "to": "schedule-backend",
        "filter": ["**/*"]
      }
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ],
      "icon": "electron/icon.ico"
    },
    "portable": {
      "artifactName": "${productName}-Portable.${ext}"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "nodemon": "^3.0.2",
    "wait-on": "^7.2.0"
  }
}
